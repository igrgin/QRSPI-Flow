#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=/dev/null
source "$ROOT_DIR/hooks/run_with_backpressure.sh"

run_silent "Format check" "make fmt-check"
run_silent "Lint" "make lint"
run_silent "Tests" "make test"
